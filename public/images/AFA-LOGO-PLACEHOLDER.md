Drop the American Fence Association logo here as `afa-logo.png`.

Recommended:
- PNG with transparent background (the hero badge sits on white/warmgray)
- ~400×160 px source (renders at h-9 ≈ 36 px tall on screen, so 2-3× pixel density is plenty for retina)
- Tight crop, minimal whitespace around the artwork

The hero references `/images/afa-logo.png`. Update the path in
`src/components/home/HeroSection.tsx` if you save it under a different
name or extension (e.g. .svg).
